#include <stdio.h>

int main(){
    int x ;
    scanf("%d", x);
    int y[47];
    int w = 0;
    y[0] = 1;
    for (int i = 0; i < x; i++)
    {  
        y[i] == w;
        w += y[i];
    }
    y.remove(y[0])
    res = " ".join(map(str, y))
    print(res)
 return 0;
}